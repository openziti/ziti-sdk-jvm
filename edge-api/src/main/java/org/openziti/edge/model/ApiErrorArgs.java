/*
 * Ziti Edge Client
 * OpenZiti Edge Client API
 *
 * The version of the OpenAPI document: 0.26.18
 * Contact: help@openziti.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package org.openziti.edge.model;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.StringJoiner;

/** ApiErrorArgs */
@JsonPropertyOrder({ApiErrorArgs.JSON_PROPERTY_URL_VARS})
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        date = "2024-05-29T11:06:12.834975-04:00[America/New_York]",
        comments = "Generator version: 7.6.0")
public class ApiErrorArgs {
    public static final String JSON_PROPERTY_URL_VARS = "urlVars";
    private Map<String, String> urlVars = new HashMap<>();

    public ApiErrorArgs() {}

    public ApiErrorArgs urlVars(Map<String, String> urlVars) {
        this.urlVars = urlVars;
        return this;
    }

    public ApiErrorArgs putUrlVarsItem(String key, String urlVarsItem) {
        if (this.urlVars == null) {
            this.urlVars = new HashMap<>();
        }
        this.urlVars.put(key, urlVarsItem);
        return this;
    }

    /**
     * Get urlVars
     *
     * @return urlVars
     */
    @javax.annotation.Nullable
    @JsonProperty(JSON_PROPERTY_URL_VARS)
    @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
    public Map<String, String> getUrlVars() {
        return urlVars;
    }

    @JsonProperty(JSON_PROPERTY_URL_VARS)
    @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
    public void setUrlVars(Map<String, String> urlVars) {
        this.urlVars = urlVars;
    }

    /** Return true if this apiErrorArgs object is equal to o. */
    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ApiErrorArgs apiErrorArgs = (ApiErrorArgs) o;
        return Objects.equals(this.urlVars, apiErrorArgs.urlVars);
    }

    @Override
    public int hashCode() {
        return Objects.hash(urlVars);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ApiErrorArgs {\n");
        sb.append("    urlVars: ").append(toIndentedString(urlVars)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    /**
     * Convert the instance into URL query string.
     *
     * @return URL query string
     */
    public String toUrlQueryString() {
        return toUrlQueryString(null);
    }

    /**
     * Convert the instance into URL query string.
     *
     * @param prefix prefix of the query string
     * @return URL query string
     */
    public String toUrlQueryString(String prefix) {
        String suffix = "";
        String containerSuffix = "";
        String containerPrefix = "";
        if (prefix == null) {
            // style=form, explode=true, e.g. /pet?name=cat&type=manx
            prefix = "";
        } else {
            // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
            prefix = prefix + "[";
            suffix = "]";
            containerSuffix = "]";
            containerPrefix = "[";
        }

        StringJoiner joiner = new StringJoiner("&");

        // add `urlVars` to the URL query string
        if (getUrlVars() != null) {
            for (String _key : getUrlVars().keySet()) {
                joiner.add(
                        String.format(
                                "%surlVars%s%s=%s",
                                prefix,
                                suffix,
                                "".equals(suffix)
                                        ? ""
                                        : String.format(
                                                "%s%d%s", containerPrefix, _key, containerSuffix),
                                getUrlVars().get(_key),
                                URLEncoder.encode(
                                                String.valueOf(getUrlVars().get(_key)),
                                                StandardCharsets.UTF_8)
                                        .replaceAll("\\+", "%20")));
            }
        }

        return joiner.toString();
    }
}
